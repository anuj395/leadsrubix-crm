- [x] Modify pgMongoose.js mappings and collection mock helper
- [x] Create and run resolve-schema-gaps.js
- [x] Run typecheck and clean up temporary files
- [x] Verify everything works live

## Phase 1: Initialize React Native Workspace
- [x] Initialize Expo project structure in `artifacts/app` folder
- [x] Configure `pnpm-workspace.yaml` to include `artifacts/app` (via `artifacts/*`)
- [x] Configure `package.json` build/typecheck script configurations to include `artifacts/app` (via `./artifacts/**`)
- [x] Set up default `tsconfig.json` and basic typescript configs inside `artifacts/app`
