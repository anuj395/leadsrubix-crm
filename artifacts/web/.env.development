# Local Development (.env.development)
# Leave VITE_API_BASE_URL empty so requests go to same-origin /api and are proxied by Vite to http://localhost:8080
VITE_API_BASE_URL=
VITE_APP_NAME=LeadsRubix
VITE_ALLOW_DEMO_FALLBACK=false
