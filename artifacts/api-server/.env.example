# .env.example
PORT=3001
NODE_ENV=development
# JWT signing key (change for production)
JWT_SECRET=supersecretkey
# MongoDB connection string (local by default)
MONGO_URI=mongodb://localhost:27017/leadrubix-crm
# Add other configurations like DB_URI, API_KEY, etc.
